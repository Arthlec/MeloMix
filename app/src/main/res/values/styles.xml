<resources>
    <!-- Base application theme. -->
    <style name="AppTheme.Green" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorBackgroundFloating</item>
        <item name="colorPrimaryDark">@color/colorForeground</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/background_material_light</item>

    </style>

    <style name="SplashTheme" parent="AppTheme.Green">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>
